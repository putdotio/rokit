diff --git a/lib/decoder.js b/lib/decoder.js
index 1982481806e5fe29bd38cb008297f35731ade20b..1495850c0dca29b49c4aeffa91a2826341baf0d4 100644
--- a/lib/decoder.js
+++ b/lib/decoder.js
@@ -1,3 +1,4 @@
+/*! Modified for rokit: bound final restart intervals and reject incomplete strict scans. */
 /* -*- tab-width: 2; indent-tabs-mode: nil; c-basic-offset: 2 -*- /
 /* vim: set shiftwidth=2 tabstop=2 autoindent cindent expandtab: */
 /*
@@ -302,7 +303,7 @@ var JpegImage = (function jpegImage() {
 
       if (componentsLength == 1) {
         component = components[0];
-        for (n = 0; n < resetInterval; n++) {
+        for (n = 0; n < resetInterval && mcu < mcuExpected; n++) {
           decodeBlock(component, decodeFn, mcu);
           mcu++;
         }
@@ -351,6 +352,10 @@ var JpegImage = (function jpegImage() {
         break;
     }
 
+    if (mcu < mcuExpected && !opts.tolerantDecoding) {
+      throw new Error("incomplete JPEG scan");
+    }
+
     return offset - startOffset;
   }
 
